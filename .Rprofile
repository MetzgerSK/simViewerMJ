library(crayon)
cat(bgBlack(paste0(rep("\u00A0",63), collapse="") %+% "\n"))
cat(bgBlack(
  white(
    yellow(" ******") %+% " Type " %+% green$bold("shiny::runApp()") %+% " to launch the viewing app. " %+% yellow("****** ") %+% "\n"
  )
))
cat(bgBlack(paste0(rep("\u00A0",63), collapse="")) %+% "\n")
